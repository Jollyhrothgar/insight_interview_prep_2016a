#! /usr/bin/env python

test_set = [10,1,2,6,6,1,5]
test_target = 8

class Solution(object):
    def generate_all_combinations(self,target):
        '''
        finds all possible combinations of numbers which can add to 
        target.
        ''' 
        print 'Hello!'
        return
    def combinationSum2(self,candidates,target):
        '''
        candidates is a list of integers
        type is the target sum of integers
        returns a list of unique integers in candidates which sum to target
        '''
        print 'Hello 2!'
        return

test = Solution();
test.combinationSum2([],12)
test.generate_all_combinations(12)


